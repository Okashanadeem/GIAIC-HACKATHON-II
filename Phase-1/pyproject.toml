[project]
name = "phase-1"
version = "0.1.0"
description = "Phase 1 of the Evolution of Todo project: an in-memory Python console app."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []